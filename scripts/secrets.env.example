# Copy this file to scripts/secrets.env (already gitignored) and fill in
# real values. Used by scripts/publish_release.sh, never by the app itself
# — the showcase app talks to public, unauthenticated GitHub Releases URLs.

# GitHub PAT with "Contents: Read and write" on just-unknown-dev/just-installer-workspace.
# Only needed to run scripts/publish_release.sh.
GITHUB_TOKEN=
